import { LoadingState } from '@/components/app/loading-state';

/** Speaking is the same header + filter + list shape as its sibling modules. */
export default function Loading() {
  return <LoadingState />;
}

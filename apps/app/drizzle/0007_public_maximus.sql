-- No-op. drizzle-kit generated this to catch its snapshot up to the schema
-- change already applied by hand in 0006_lesson_progress_fk.sql (a --custom
-- migration doesn't snapshot the schema.ts edits made alongside it, so this
-- diff reappeared here). The DB already matches the current schema; this
-- migration exists only to reconcile the tracked migration history.
SELECT 1;

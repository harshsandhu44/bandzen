# The site's own public origin. It is what `metadataBase`, `sitemap.xml` and
# `robots.txt` build absolute URLs from. Unset falls back to
# https://docs.bandzen.com, which is right in production and wrong locally only
# for those three things.
NEXT_PUBLIC_DOCS_URL=

# Neon — the same database as apps/app (pooled connection string)
DATABASE_URL=postgresql://<user>:<password>@<host>/<db>?sslmode=require

# Clerk — same instance as apps/app, Dashboard → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# Comma-separated emails treated as admin regardless of Clerk publicMetadata.
# This is how the very first admin bootstraps themselves (there is no one
# else to grant them the role via the /teachers screen), and it stays set
# permanently afterward as break-glass access if publicMetadata is ever
# cleared or wiped.
ADMIN_EMAILS=
